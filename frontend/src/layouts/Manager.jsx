import { managerNavbarLinks } from "../constants";
import AppLayout from "../routes/layout";

const ManagerLayout = () => <AppLayout navbarLinks={managerNavbarLinks} />;

export default ManagerLayout;
